import { Text, View } from "react-native";
import { HeaderFilterButton } from "../../../../components/Buttons";

export default function SelectExistent() {
    return (
        <View> 
            <HeaderFilterButton title={"Criar orçamento"} />
        </View>
    );
}